# Инструкция по входу в приложение под администратором

## 📋 Общая информация

В приложении **FinanceFlow** реализована система ролей с двумя типами пользователей:
- **Пользователь (USER)** - обычный пользователь с доступом к личным финансовым данным
- **Администратор (ADMIN)** - пользователь с расширенными правами доступа к админ-панели

## 🔐 Как зайти под администратором

### Способ 1: Регистрация нового администратора

1. **Откройте приложение FinanceFlow**
2. **На экране входа/регистрации** нажмите на кнопку "Регистрация" (или "Зарегистрироваться")
3. **Введите следующие данные:**
   - **Email:** `admin@test.com`
   - **Пароль:** (любой пароль, который вы запомните, например: `admin123`)
   - **Имя:** (любое имя, например: `Администратор`)
4. **Нажмите "Зарегистрироваться"**

> ⚠️ **Важно:** Email должен быть **точно** `admin@test.com` (регистр не важен, но лучше использовать строчные буквы). Система автоматически присвоит роль `ADMIN` пользователю с таким email.

### Способ 2: Вход существующего администратора

Если администратор уже зарегистрирован:

1. **Откройте приложение FinanceFlow**
2. **На экране входа** введите:
   - **Email:** `admin@test.com`
   - **Пароль:** (пароль, который вы указали при регистрации)
3. **Нажмите "Войти"**

## 🎯 Как проверить, что вы вошли как администратор

После успешного входа:

1. **Перейдите в раздел "Профиль"** (иконка профиля в нижней навигации)
2. **Проверьте информацию о пользователе:**
   - Должно отображаться: **"Роль: Администратор"** или **"Role: ADMIN"**
   - Email должен быть: `admin@test.com`

3. **В главном меню** (или в профиле) должна появиться кнопка/раздел **"Админ-панель"** или **"Admin Panel"**

## 🛠️ Возможности администратора

После входа под администратором вы получаете доступ к следующим функциям:

### 1. **Управление пользователями**
   - Просмотр списка всех зарегистрированных пользователей
   - Блокировка/разблокировка пользователей
   - Сброс пароля пользователя (отправка email с инструкциями)

### 2. **Управление системными категориями**
   - Просмотр всех системных категорий (видимых всем пользователям)
   - Добавление новых системных категорий
   - Редактирование существующих системных категорий
   - Удаление системных категорий

### 3. **Агрегированная статистика**
   - Общее количество пользователей
   - Количество активных пользователей
   - Количество транзакций за неделю
   - Процент пользователей с более чем 5 транзакциями
   - Самая популярная категория расходов
   - Среднее количество транзакций на пользователя

## 📱 Как открыть админ-панель

1. **Войдите под администратором** (email: `admin@test.com`)
2. **Откройте главный экран приложения**
3. **Найдите кнопку "Админ-панель"** в одном из мест:
   - В меню профиля (иконка профиля → "Админ-панель")
   - В главном меню (если есть отдельная секция)
   - В навигации приложения

4. **Нажмите на "Админ-панель"**

> 💡 **Примечание:** Если вы не видите кнопку "Админ-панель", убедитесь, что:
> - Вы вошли под email `admin@test.com`
> - Роль пользователя установлена как `ADMIN`
> - Приложение полностью загрузилось

## 🔧 Технические детали

### Определение роли администратора

Роль администратора определяется автоматически при регистрации на основе email:

```kotlin
role = if (email == "admin@test.com") UserRole.ADMIN else UserRole.USER
```

Это происходит в файле `AuthRepository.kt` в методах:
- `register()` - при регистрации
- `login()` - при входе (если пользователь не найден в Firestore)

### Структура данных пользователя

```kotlin
data class User(
    val id: String,
    val email: String,
    val name: String,
    val role: UserRole = UserRole.USER, // USER или ADMIN
    val createdAt: Long,
    val isBlocked: Boolean = false
)
```

## ⚠️ Важные замечания

1. **Безопасность:**
   - В продакшене рекомендуется использовать более безопасный способ определения администраторов (например, через Firebase Custom Claims или отдельную таблицу администраторов)
   - Текущая реализация подходит только для тестирования

2. **Firebase:**
   - Убедитесь, что Firebase настроен правильно
   - Пользователь должен быть синхронизирован с Firestore для корректной работы

3. **Локальная база данных:**
   - При первом запуске приложения создаются системные категории по умолчанию
   - Данные синхронизируются между локальной БД (Room) и Firestore

## 🐛 Решение проблем

### Проблема: Не могу войти как администратор

**Решение:**
1. Убедитесь, что email точно `admin@test.com` (без пробелов, правильный регистр)
2. Проверьте, что пароль введен правильно
3. Если пользователь уже существует, попробуйте сбросить пароль через Firebase Console

### Проблема: Не вижу админ-панель

**Решение:**
1. Проверьте роль пользователя в профиле
2. Перезапустите приложение
3. Проверьте логи приложения на наличие ошибок

### Проблема: Роль не устанавливается как ADMIN

**Решение:**
1. Удалите приложение и установите заново
2. Зарегистрируйтесь заново с email `admin@test.com`
3. Проверьте код в `AuthRepository.kt` - условие должно быть `email == "admin@test.com"`

## 📞 Дополнительная информация

Для получения дополнительной помощи или сообщения об ошибках, обратитесь к разработчику приложения.

---

**Версия документа:** 1.0  
**Дата обновления:** 2024




